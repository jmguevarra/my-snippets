<?php
/*
 * Validation is when you check/analyze the input/data before passing to the database.
 * 
 */

//Common Build in PHP function

/* isset() - check whether the variable is null*/ 


/* empty() */


/* strlen() - check length of the string variable */


/* preg_match() */


/* count() - count the length of in interger */


/* in_array - check whether the variable is an array*/


//Notes for creating functions name use: 
//is - check to validate e.g is_us_phone(), is_valid()
//has - check when item is carrying something e.g has_value() has_class()
//in - check the type e.g in_array() in_category()
//post - after the present function  pre_submission() submission() post_submission()
//pre  - before the present function  pre_submission() submission() post_submission()
//get  - get a return value get_name() get_lastname()
//set - setting an item set_name, set_newFields()
